# v0.1.0 Release Notes Draft

状态：未发布。

发布前必须确认：

- `go test ./...` 通过。
- `go vet ./...` 通过。
- `docs/` 和 `examples/` 与当前 API 同步。
- 发布检查项已全部确认。
- 未提前创建 tag 或发布包。

Go 基线：`go 1.27`。

Module：`github.com/duxweb/runa`。
