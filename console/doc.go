// Package console exposes the optional Runa console panel system.
//
// Console intentionally centers on Panel objects collected by Registry rather
// than a single business Registry-only API because it is an aggregate UI layer.
package console
